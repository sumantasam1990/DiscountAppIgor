import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.goodyellow.discountapp',
  appName: 'Cartingly',
  webDir: 'www',
  bundledWebRuntime: false,
};

export default config;
